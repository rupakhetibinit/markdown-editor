import React from 'react';

type Props = {};

function Header({}: Props) {
	return (
		<div className='prose m-0'>
			<h1>Simple Markdown Editor written in React</h1>
		</div>
	);
}

export default Header;
